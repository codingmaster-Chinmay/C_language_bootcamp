#include<stdio.h>
#include<stdlib.h>
int main()
{
int money;
int grade;
printf("Enter money and grade: \n");
scanf("%d%d", &money,&grade);
//we knoe that we can take both the %d at a one time and can take both the input varibles at a moment
if(money<50 && grade>90)
//Do something 
 if(money<50 || grade> 90)
 // Do something
 
 return 0;
 }
